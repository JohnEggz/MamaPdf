[project]
name = "johnmamapdfv2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "JohnEggz", email = "avocadojuiceav@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "eel>=0.18.2",
    "python-calamine>=0.8.2",
    "rich>=15.0.0",
    "typst>=0.15.0",
]

[project.scripts]
johnmamapdfv2 = "johnmamapdfv2:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
